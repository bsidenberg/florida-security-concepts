# Post-deploy verification — production release, 2026-09-15

Release: PR #1 merged as `0498794`; Vercel Production deployment created 2026-09-16T00:40:23Z (2026-09-15 20:40 EDT). Site: https://www.floridasecurityconcepts.com/.

Owner-run steps (Vercel variables, SQL, Plausible goals, merge, live form test authorization, mailbox and Resend confirmation) were performed by Brian. Agent work here was read-only verification plus the authorized controlled live form test. Times are EDT unless marked UTC.

## 1. Configuration verification (read-only, before merge)

- Vercel project variables: exactly 10, System Environment Variables enabled. `FSC_ADMISSION_HMAC_KEY` **Production only**, added 2026-09-15. `LEAD_DELIVERY_MODE` Production. `RESEND_API_KEY`, `PRIME_SUPABASE_URL`, `PRIME_SUPABASE_SERVICE_ROLE_KEY`, `PRIME_ACCOUNT_SLUG`, `LEAD_NOTIFICATION_FROM`, `LEAD_NOTIFICATION_TO` all Production only — Preview scope removed (OWNER-LAUNCH-STEPS 1.4 done). `NEXT_PUBLIC_SITE_URL` and `NEXT_PUBLIC_PLAUSIBLE_SCRIPT_URL` remain Production and Preview; neither is a credential and preview isolation was separately verified.
- **Not verifiable by agents:** the values of `FSC_ADMISSION_HMAC_KEY` and `LEAD_DELIVERY_MODE` (hidden by design), and the Sensitive flag on the key. Proven instead by the live test below.
- Supabase `olpyqfuphiwdongzmazi` after owner SQL: `fsc_private` contains exactly `assessment_receipts`, `assessment_effects`, `admission_counters`, RLS enabled, 0 rows at baseline; `pg_cron` 1.6.4 installed. Security advisor after DDL: only three new INFO `rls_enabled_no_policy` notices on the `fsc_private` tables (intended deny-all; access is through server-side functions), and no `function_search_path_mutable` finding against any new function. The two WARN findings are pre-existing `public` functions.
- Owner health check output (Brian): `job_exists` true, `job_active` true, `job_schedule_matches` true, `job_command_matches` true, `last_successful_run_at` 2026-09-16 00:23:00+00, `receipts_due_for_purge` 0, `healthy` true. Cross-check: current time at reading was 00:24:02 UTC, so the job had run 62 seconds earlier, consistent with the per-minute schedule.
- Plausible: all eight SPEC goals exist as Custom Events with exact names. Four pre-existing default goals (`Form: Submission`, `File Download`, `Outbound Link: Click`, `404`) also exist and receive nothing from this release.
- Analytics script: configured URL `https://plausible.io/js/pa-z8wpGBQzz21OWo7apGbdN.js` matches the required `pa-*.js` pattern. Re-downloaded 2026-09-15: SHA-256 `699a37594f53cf5fd1026e3e442d29119c6901cfd3a696dd5cec20d7cc52a8a4`, identical to the copy pinned in `tests/release/support/live-tracker.ts`, so the S-006 payload evidence still applies.

## 2. Read-only production checks (after merge)

- HTTP 200: `/`, `/contact`, `/services`, `/industries/hoa-gated-communities`, `/robots.txt`, `/sitemap.xml`.
- Indexing restored on production: `<meta name="robots" content="index, follow">` on `/` and `/contact`, no `X-Robots-Tag` override. Preview noindex did not leak.
- Canonicals: `https://www.floridasecurityconcepts.com` and `…/contact` — confirms the `htmlLimitedBots` fix renders dynamic `/contact` metadata in production.
- `robots.txt`: `User-Agent: *`, `Allow: /`, `Disallow: /api/`, plus Host and Sitemap lines. `sitemap.xml`: 38 `<loc>` entries, matching launch scope.
- Apex `floridasecurityconcepts.com` → 308 → www. `GET /api/leads` → 405 `METHOD` with `Allow: POST`.
- Owner-approved OD-07 disclosure (D-027 final wording) renders on production `/contact`.
- Console: zero messages (no errors or warnings) on a fresh homepage load with tracking armed before navigation.

## 3. Live `/api/event` payload check (read-only, no submission)

Entry by link from `https://example.com` to `/contact?utm_campaign=spring&utm_source=Newsletter%20Blast!&email=test@example.com&gclid=abc123`; request bodies captured in-page.

| Event | `u` | `r` | `p` |
|---|---|---|---|
| `engagement` (tracker built-in) | `https://www.floridasecurityconcepts.com/contact?utm_source=other&utm_campaign=spring` | absent | none |
| `assessment_form_start` (ours) | same sanitized URL | `https://example.com` (origin only, no path) | `{ page_template: "contact" }` |

`email=` and `gclid=` were dropped; `utm_source=Newsletter Blast!` normalized to `other` (fails the allowed pattern); clean `utm_campaign=spring` retained; origin forced to canonical www. Canary scan of both raw bodies found no contact values or free text. Note for reporting: the tracker also emits its own `engagement` event, which is not one of the eight SPEC events; it carries a sanitized `u` and no properties.

## 4. Controlled live form test (authorized by Brian 2026-09-15)

Submitted once through the real production form, then replayed the byte-identical body (same request ID) to exercise the retry path. Request ID ends `5362eecc`.

Payload: name "Brian Sidenberg (TEST)", email brian@floridapolebarn.com (owner-specified), phone (352) 282-0692 (FSC's own published number, deliberately not a third party), HOA / gated community, Orlando, Maintenance / service, message "TEST — Brian, S-006 live test", contact method Email, urgency Not specified.

- Submission 1: HTTP 200, `ok: true`, "Request received. This is an assessment request, not a confirmed appointment." Confirmation state rendered.
- Submission 2 (identical body, same request ID): HTTP 200, **same request ID returned in 287 ms** — the stored receipt, not a re-send.

Database, baseline → after both POSTs (Supabase metadata row counts):

| Table | Before | After |
|---|---|---|
| `fsc_private.assessment_receipts` | 0 | 1 |
| `fsc_private.assessment_effects` | 0 | 3 |
| `fsc_private.admission_counters` | 0 | 1 |
| `public.leads` | 580 | 581 |

One receipt, three effects (company notification, customer confirmation, Prime lead), one admission count (the retry was exempt), and exactly one Prime lead from two POSTs.

Owner-confirmed outcomes (agents have no access to either mailbox; the M365 connector is signed in as info@floridapolebarn.com, `brian@floridapolebarn.com` returned ErrorAccessDenied and `info@floridasecurityconcepts.com` is outside that tenant):

- Exactly one company notification at info@floridasecurityconcepts.com, 8:48 PM, correct details.
- Resend log: both messages **Delivered once each**, no second copy from the replay.
- Reconciliation report (`sql/fsc-receipt-reconciliation-report.sql`, SHA-256 `a83df65be5d51c3b14614c9e84d5a95a1136b4244431a21db3106a74ce891eb1`): success, **no rows returned, nothing unresolved**.

**AC-06 is therefore satisfied on real infrastructure:** duplicate prevention, single company notification, single Prime lead and same-ID retry reconciliation were observed in production against the real providers, not only in isolated tests. This also proves what isolated tests could not: PostgREST exposure of the new `fsc_private` RPCs to the service role with a reloaded schema cache, and a working `FSC_ADMISSION_HMAC_KEY` and `LEAD_DELIVERY_MODE` (a missing key would have refused the new request with 503; a wrong mode would not have produced this effect set).

## 5. Open finding — customer confirmation deliverability (not a release defect)

The customer confirmation to brian@floridapolebarn.com was **sent and Delivered per Resend**, then held tenant-side by the recipient's anti-spoof filtering. Owner-confirmed sender authentication for floridasecurityconcepts.com in Resend: domain Verified, DKIM TXT `resend._domainkey` Verified, SPF MX and TXT on the send subdomain Verified, sending enabled.

So this is inbox placement at the receiving tenant, not a send failure and not caused by any code in this release. It nevertheless affects every customer who receives a confirmation. Follow-up drafted, not run: `harness/sessions/S-DELIV-001-dmarc-alignment-contract.md`.

## 6. Limitations

- Row counts come from Supabase metadata and are planner estimates, not `count(*)`. The deltas are consistent with the receipt, Resend log and reconciliation report.
- Effect states were not read directly; "nothing unresolved" comes from the reconciliation report, which returns only unresolved rows.
- The live test exercised one path: success with retry. Ambiguous-provider, expiry, cutoff and admission-limit behavior in production remain covered only by the isolated S-005 suites.
- The payload check itself sent two real analytics events (one visit from example.com appears in Plausible).
- The test lead row in `public.leads` is real data and remains for the owner to remove if desired; the `fsc_private` copy purges after seven days via the cleanup job.
- Physical device, real screen reader and field Core Web Vitals remain not performed.

## 7. Addendum — deliverability scope narrowed, 2026-09-15

Brian ran a second test submission using an **external** email address (not a domain he controls). The customer confirmation was received normally.

Conclusion: the earlier quarantine is confined to same-tenant recipients, where the receiving tenant applies stricter anti-spoof rules to outside mail referencing a familiar display name. Customer confirmations reach ordinary recipients as intended. Sending, authentication (DKIM, SPF, domain Verified) and Resend delivery were never in question.

Effect: OD-08 and S-DELIV-001 are downgraded to low priority. No launch impact, no code change, and no DNS change proposed. Source: owner observation reported 2026-09-15; the second submission's provider log and inbox were checked by Brian, not by an agent.
