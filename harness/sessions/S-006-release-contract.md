# S-006 release contract — analytics privacy, release QA and PR readiness, 2026-09-14

Authority: SPEC §7–§8, §10 AC-08..AC-13; HARNESS C-06 and §5; SESSIONS S-006 packet; publication approval already recorded. Depends on S-005 machine gate + independent review. Launch scope is the 38 existing canonical routes plus the accepted theme (S-003/S-004 deferred). No new tracking service, consent banner, cookies, pixels or session replay. Existing Plausible site only.

## Analytics design (builder)

- Single module `lib/analytics/events.ts` owning the eight SPEC events and a strict allowlist: `assessment_cta_click{placement,service_category}`, `assessment_form_start{page_template}`, `assessment_submit_attempt{service_category,property_category,region,urgency_category}`, `assessment_validation_error{error_class,field_category}`, `assessment_delivery_error{error_class}`, `Lead Submitted{service_category,urgency_category}` (existing name; once per logical request ID, never on replayed retry UI state), `emergency_call_click{placement}`, `maintenance_interest_click{placement}`. Every property value is mapped through enumerations (unknown → `other`/`unknown`); no free text, contact fields, city strings (region = orlando/tampa/other/unknown from the existing location data only), request IDs, full URLs or query strings. Analytics exceptions are swallowed and never alter form/delivery state.
- Pageviews: disable automatic capture and send manual pageviews with a sanitized URL = production origin + canonical pathname (known route inventory; unknown → `/404`) + only `utm_source`/`utm_medium`/`utm_campaign` normalized to `^[a-z0-9._-]{1,40}$` (else `other`). Referrer reduced to origin only (or removed) via the tracker's `transformRequest` if the pa- script supports it; builder must confirm from the installed/official tracker source and record the evidence. If it cannot be controlled, record the exact residual behavior as a limitation for Brian rather than claiming sanitization.
- Script only renders when `NEXT_PUBLIC_PLAUSIBLE_SCRIPT_URL` is an `https://plausible.io/js/pa-*.js` URL and the environment is neither local preview nor hosted preview (existing guards). Build-time and runtime both off in preview.
- Form/CTA/phone hooks are minimal attribute/handler additions; no copy or visual change.

## Release tests (test-guard)

- `test:release` (Playwright JSON report; registered in harness/verification.json by the orchestrator): Chromium, Firefox and WebKit projects against the compiled isolated local server covering home → contact assessment success (local receipt), validation errors with focus/summary, known failure (FSC_LOCAL_FAILURE server), unknown/504 retry with the same request ID (route interception), emergency direct entry/back-forward consistency, mobile 390 and desktop 1440 overflow, keyboard-only submit, 200% zoom/reflow spot checks, reduced motion, axe (no critical/serious) on representative route families.
- Analytics payload capture: serve the official Plausible tracker build from a pinned dev dependency (e.g. `@plausible-analytics/tracker`) at the intercepted script URL in a test-only build with a synthetic script URL; intercept `/api/event` POSTs; assert event names, property keys/values against the allowlist, sanitized `u`/`r`, zero contact values, `Lead Submitted` exactly once per logical request, no analytics in local/hosted-preview builds. Never contact plausible.io.
- Lighthouse: pinned `lighthouse` dev dependency using Playwright's Chromium, mobile preset, three runs per representative URL (home, contact, services, one industry, one area, one article) against the compiled loopback server; store raw JSON reports and a median summary under harness/evidence; fail if the evidence manifest is missing. Targets Performance ≥90, Accessibility ≥95, Best Practices ≥95, SEO ≥95 are reported honestly; a miss is a finding for repair or an explicit documented exception, never a silent pass. Note local-loopback caveats (noindex on local preview affects SEO audit — measure with an isolated non-preview compiled build that still has delivery and analytics disabled, or record the exception).
- Existing floor preserved; physical device, real screen reader and field Core Web Vitals are recorded as not available unless actually performed.

## Documentation and PR (orchestrator)

Traceability matrix AC-01..AC-13 → evidence, updated DEPLOYMENT/LAUNCH-HANDOFF, owner-only launch steps (SQL order, FSC_ADMISSION_HMAC_KEY Production-only entry, Preview credential scoping, LEAD_DELIVERY_MODE=resend+supabase, schedule/health observation, controlled receipt test authorization), rollback. Feature-branch push only after hosted-preview refusal/noindex/analytics-off is machine-verified; PR body includes summary (SEO commits 3d6c3fd/f25a5b3, accepted theme, security patch, receipt/abuse safeguards), exact exit-0 log paths, preview instructions and limitations. Brian alone merges.

## Execution addendum — exact mechanics (orchestrator, 2026-09-14)

Ownership: Builder (Opus) owns lib/analytics/**, app/layout.tsx analytics wiring, minimal event hooks in existing components (LeadCaptureForm, CTA/hero/header/footer phone and assessment links, maintenance links), scripts/local-server.mjs (new measurement mode) and scripts/verify.ps1 (test:release report contract only). Test-guard (Opus) owns tests/release/**, playwright.release.config.ts, package.json devDependencies/scripts + lockfile, and gate self-tests for the verify.ps1 change. Orchestrator owns harness/verification.json and evidence.

1. Gate: verify.ps1 currently requires every `test:*` other than test:e2e to report in vitest format. Change only that rule so `test:release` uses the Playwright JSON report contract (same zero/failed/skipped/flaky rejection). Orchestrator registers `test:release` in verification.json as `node node_modules/@playwright/test/cli.js test --config playwright.release.config.ts` with reportEnv FSC_RELEASE_REPORT. Gate self-tests must cover the new rule.
2. Launcher measurement mode: `scripts/local-server.mjs --compiled --measure` builds into a separate distDir (never overwrite the preview `.next` concurrently) with FSC_LOCAL_PREVIEW unset (so robots/meta are production-equivalent and indexable), LEAD_DELIVERY_MODE unset (API returns 503 before any provider), all provider variables blank, hosted markers refused as today, loopback bind, network guard preloaded. Optional `--analytics-fixture` sets NEXT_PUBLIC_PLAUSIBLE_SCRIPT_URL to the synthetic `https://plausible.io/js/pa-fsc-release-fixture.js` for payload-capture tests only; browsers in those tests must route that URL to the pinned official tracker build and intercept `https://plausible.io/api/event`. No browser or Node process may reach any non-loopback host: Playwright contexts route-abort all non-loopback requests except the intercepted fixtures; Lighthouse's Chrome runs with host-resolver rules mapping every non-loopback host to unreachable.
3. Analytics must render nothing when the script URL is not an `https://plausible.io/js/pa-*.js` URL, and nothing in local preview or hosted preview.
4. Release tests run after the gate's build and e2e stages; they manage their own measurement builds and must not reuse or corrupt the e2e build. Keep total runtime under 25 minutes; the orchestrator runs the gate with an extended -CheckTimeoutSeconds.
5. Lighthouse evidence: raw JSON per run plus `harness/evidence/S-006-lighthouse-summary.json` (tool versions, Chromium version, preset, URLs, per-run scores, medians). Written by the release suite into a per-run folder named from FSC_RELEASE_REPORT's run id, never overwriting prior runs. Scores below target fail the suite unless recorded as an explicit, reviewed exception in harness/DECISIONS.md by the orchestrator — the test must not hardcode lowered thresholds.
