// Analytics payload capture against the pinned npm tracker core behind the loader adapter (Chromium project only).
import { defineAnalyticsSuite } from './support/analytics-suite';

defineAnalyticsSuite('npm');
